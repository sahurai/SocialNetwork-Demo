namespace SocialNetwork.Core.Enums
{
    public enum GroupRole
    {
        Admin,      // The admin of the group (creator)
        Manager,    // The manager of the group (with certain permissions)
        Member      // A regular member of the group
    }
}
